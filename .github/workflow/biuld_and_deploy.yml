name: Deploy React App
on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      # - name: Use Node.js
      #   uses: actions/setup-node@v4

      - name: Build
        run: npm -v
        # run: |
        #   npm ci
        #   npm run build